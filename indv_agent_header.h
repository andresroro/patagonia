/**
 * \file  indv_agent_header.h
 * \brief Header for agent type memory access.
 */

/** \def INDVID
 * \brief Direct access to indvID of indv agent memory variable. */
#define INDVID (current_xmachine_indv->indvID)
/** \def CID
 * \brief Direct access to cID of indv agent memory variable. */
#define CID (current_xmachine_indv->cID)
/** \def CAL_NEEDS
 * \brief Direct access to cal_needs of indv agent memory variable. */
#define CAL_NEEDS (current_xmachine_indv->cal_needs)
/** \def ICALORIES
 * \brief Direct access to icalories of indv agent memory variable. */
#define ICALORIES (current_xmachine_indv->icalories)
/** \def PATCHID
 * \brief Direct access to patchID of indv agent memory variable. */
#define PATCHID (current_xmachine_indv->patchID)
/** \def AGE
 * \brief Direct access to age of indv agent memory variable. */
#define AGE (current_xmachine_indv->age)
/** \def SEX
 * \brief Direct access to sex of indv agent memory variable. */
#define SEX (current_xmachine_indv->sex)
/** \def PREGNANT
 * \brief Direct access to pregnant of indv agent memory variable. */
#define PREGNANT (current_xmachine_indv->pregnant)
/** \def LEAD
 * \brief Direct access to lead of indv agent memory variable. */
#define LEAD (current_xmachine_indv->lead)
